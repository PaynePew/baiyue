# VCS / tooling
.git
.github
.husky
.vscode
.cache

# deps + build outputs (reinstalled / regenerated inside the image)
node_modules
build
public/build
app/styles/app.css
.netlify

# secrets — never bake into the image (provided via .env.prod on the box)
.env
.env.*
!.env.example

# misc
*.md
npm-debug.log*
